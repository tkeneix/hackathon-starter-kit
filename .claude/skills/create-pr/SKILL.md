---
name: create-pr
description: 実装・テスト・レビューが終わった差分から、課題・解決策・テスト・レビュー観点の構成で PR 本文を書き、tools/bin/flow.sh pr で GitHub に PR を作成する。作業の区切りで PR を出すときに使う。
---

# PR を構造化して作成する

PR 作成はこのリポジトリでは都度の許可不要（`CLAUDE.md`）。GitHub 上の PR が Step 7（人間レビュー）の場になる。
承認とマージは、ユーザーの明示的な指示があるときだけ行う。

## 1. 差分と経緯を集める

- `git diff origin/main...HEAD` で変更全体を確認する。
- 対応するプラン（`docs/plans/`）、設計判断、Step 2・3・6 のレビュー記録、テスト結果を読む。
- 実際の差分やコマンド出力で確認できない効果・検証結果は書かない。
- 秘密情報・個人データ・生のログを本文に書かない（公開リポジトリになり得る）。必要なら伏せ字にし、構造だけを書く。

## 2. PR 本文を書く

`.github/pull_request_template.md` の見出しに沿って書く。

- **課題**: 何を解決するか。背景と、対応するプラン・Issue へのリンク。
- **解決策**: 主要な変更、設計判断とトレードオフ（1〜2 行）。データの流れ。
- **テスト**: 追加・変更したテスト、Red→Green の確認、実行したコマンドと結果。
- **レビュー観点**（Step 7 の判断材料）: 特に見てほしい箇所、プランとの差分・未対応事項、
  壊れた場合の影響範囲とロールバック方法、受け入れたリスク、機械的に確認できず「不明」な点。

## 3. 作成する

- 本文を `.review/pr-body.md`（gitignore 対象）に書き出し、`tools/bin/flow.sh pr -t "<タイトル>" --body-file .review/pr-body.md`
  を実行する（ゲート → push → PR 作成）。本文をシェル引数に直接埋め込まない（`$(...)` 等が展開され得る）。
  `gh pr create` を直接使わない。
- 作成した PR の URL と CI の結果（`gh pr checks <番号> --watch`）を報告する。
- 承認（`flow.sh approve`）とマージ（`flow.sh merge`）は、ユーザーの指示があるまで実行しない。
